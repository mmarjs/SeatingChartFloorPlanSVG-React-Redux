#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../svg-to-jsx/bin/svg-to-jsx" "$@"
  ret=$?
else 
  node  "$basedir/../svg-to-jsx/bin/svg-to-jsx" "$@"
  ret=$?
fi
exit $ret
